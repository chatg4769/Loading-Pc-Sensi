{
  "name": "sensi-backend",
  "scripts": {
    "build": "cp src/index.js public/_worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.4",
    "hono": "^4.8.1"
  },
  "devDependencies": {
    "wrangler": "^4.20.3"
  }
}
